<div class="col-md-6 col-md-offset-3">
    <% if sign_in? %>
        <%= form_for([@recipe, @recipe.comments.build]) do |f| %>
            <%= f.text_area :content, class:"form-control" %><br>
            <%= f.submit "Wyślij komentarz" %>
        <% end %>
    <% end %>
</div>